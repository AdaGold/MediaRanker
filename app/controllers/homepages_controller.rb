class HomepagesController < ApplicationController
  # def index
    # take top ten books
    # @books = Work.top_ten("book")
    # take top ten albums
    # @albums = Work.top_ten("album")
    # take top ten movies
    # @movies = Work.top_ten("movie")
    # find the one with highest votes
  # go to each category and get the highest Work
  # highest_book = @books.first
  # highest_album = @albums.first
  # highest_movie = @movies.first
  # compare each category to find the one with the most votes
  # @spotlight = [highest_book, highest_album, highest_movie].max_by {|work| work.votes.length}

  
  # keep it in a variable to display in view.
  # end
end